// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
}
 
datasource db {
  provider = "postgresql"
  url = "postgres://default:fBNblEW10ChR@ep-solitary-night-a47dp3gm-pooler.us-east-1.aws.neon.tech/verceldb?pgbouncer=true&connect_timeout=15&sslmode=require"
  directUrl = "postgres://default:fBNblEW10ChR@ep-solitary-night-a47dp3gm.us-east-1.aws.neon.tech/verceldb?sslmode=require"
}

model Product {
  id       Int      @id @default(autoincrement())
  name     String
  quantity Int
  price    Float
  image    String?
  created_at DateTime @default(now())
  updated_at DateTime @updatedAt
  @@map("product")
}
